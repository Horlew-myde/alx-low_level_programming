#include <stdio.h>

/**
 * main - char number combo
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x= '0'; x < '10'; i++ )
	{
		putchar(x);
		if (n != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
